@model IEnumerable<RccgChildrenManagement.Models.User>

@{
    ViewData["Title"] = "User  List";
    Layout = "~/Views/Shared/LayoutNew.cshtml";
}
<br />
<br />
<br />
<br />
<br />
<CENTER><h4 style="margin-top: 30px; font-size:55px;">ADMIN USER LIST   </h4></CENTER>
<h4 style="text-align :right;">@DateTime.Now.ToString("dd MMMM, yyyy"), @DateTime.Now.ToShortTimeString()</h4>
<div class="panel-body">
    <div class="row">
    </div>
    <div class="form-border table-responsive" style="margin-top:40px;">
        @if (Model.Count() > 0)
        {
            <table class="table table-striped table-hover" id="jayb1" border="1">
                <thead style="background-color:azure;">
                    <tr>
                        <th>
                            SNo
                        </th>
                        <th>
                            User Name
                        </th>
                        <th>
                            Home Address
                        </th>
                        <th>
                            EmailAddress
                        </th>
                        <th>
                            Phone No
                        </th>
                        <th>
                            Member_Visitor
                        </th>
                        <th>
                            Kids Number
                        </th>
                        <th>
                            Registered
                        </th>

                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model)
                    {
                        var Id = item.UserId;

                        <tr>
                            <td>
                                @Html.DisplayFor(modelItem => item.UserId)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.FirstName)

                                @Html.DisplayFor(modelItem => item.LastName)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.Address)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.EmailAddress)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.PhoneNo)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.Member_Visitor)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.Kidnumber)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.Created)
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        }
        else
        {
            <center> <p style="color:black; font-size:35px;">No user is created</p></center>
        }
    </div>
    @*<div class="text-center">

            @if (User.IsInRole("SuperAdmin") | User.IsInRole("Admin"))
            {
                <a href="~/Roaster/Index1/" class="btn btn-primary bg-c-blue" style="font-size:35px"><i class="fas fa-plus"></i>&nbspView Roaster Report</a>
            }
        </div>*@
   

    @section Scripts{
        <script>


            $(document).ready(function () {
                $("#jayb1").DataTable({
                    dom: 'Bfrtip',
                    buttons: [
                        'excel', 'pdf', 'print'
                    ]
                });

                $("#dialog-form").dialog({
                    modal: true
                });

            });
        </script>
    }
